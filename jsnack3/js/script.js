let sum = 0;
for (let i = 1 ; i <= 10 ; i++) {
    sum += i;
}
console.log(`la somma è ${sum} e la media è ${sum/10}`);

// Gauss
const sum2 = (10+1)*10/2;
console.log(`(Gauss) la somma è ${sum2} e la media è ${sum2/10}`);